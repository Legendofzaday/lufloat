[package]
name = "lufloat"
version = "0.0.1"
edition = "2024"

[lib]
name = "lufloat"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.29.2", features = ["extension-module"] }
numpy = "0.29"

[profile.release]
codegen-units = 1
lto = true
panic = "abort"
strip = true